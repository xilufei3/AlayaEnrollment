# Public environment variables consumed by the Next.js frontend.
# Copy to web/.env.local (or set in deployment pipeline) and adjust as needed.

NEXT_PUBLIC_API_URL=/api
NEXT_PUBLIC_ASSISTANT_ID=agent
NEXT_PUBLIC_API_SHARED_KEY=
NEXT_PUBLIC_LANGSMITH_API_KEY=
